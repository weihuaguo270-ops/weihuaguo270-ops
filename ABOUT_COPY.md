# 仓库 About / Bio 建议文案（需在 GitHub 网页或 `gh` 更新）

> 本地 README 已统一为学习/实验口径；下面用于同步 **Profile bio** 与各仓 **About description**。

## Profile bio（建议）

```text
哈工程计算机 · 大三 · 大模型算法 / Agent / LLM 评测
学习实现：ReAct Agent、Transformer Attention、LLM Eval
```

## 各仓 About description（建议）

| 仓库 | 建议简介 |
|------|----------|
| react-agent | `ReAct Agent 学习实现 — 纯 Python 运行时 + LangGraph 对照；RAG / MCP / 评测` |
| llm-eval-engine | `LLM 评估实验框架 — Process Reward、动态评分标准、自适应 Eval Loop` |
| transformer-attention | `Transformer Attention 教学实现 — MHA、GQA、MLA、Speculative Decoding` |
| trace-debugger | `Agent 轨迹分析小工具 — Harness 根因分析、回放、批量扫描` |

### 命令（需有效 `gh auth`）

```bash
gh repo edit weihuaguo270-ops/react-agent --description "ReAct Agent 学习实现 — 纯 Python 运行时 + LangGraph 对照；RAG / MCP / 评测"
gh repo edit weihuaguo270-ops/llm-eval-engine --description "LLM 评估实验框架 — Process Reward、动态评分标准、自适应 Eval Loop"
gh repo edit weihuaguo270-ops/transformer-attention --description "Transformer Attention 教学实现 — MHA、GQA、MLA、Speculative Decoding"
gh repo edit weihuaguo270-ops/trace-debugger --description "Agent 轨迹分析小工具 — Harness 根因分析、回放、批量扫描"
```

Profile bio：GitHub → Settings → Profile → Bio。
